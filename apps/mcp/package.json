{
  "name": "@sketchforge/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server that pushes WorkplaneShape scenes into the SketchForge editor via the /api/scene bridge.",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.15.18",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
